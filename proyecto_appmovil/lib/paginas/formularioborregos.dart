import 'package:flutter/material.dart';

class formularioborregos extends StatelessWidget {
  const formularioborregos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registro de Borrego'),
      ),
      body: Center(
        child: ElevatedButton(
          child: Text('Regresar'),
          onPressed: () {
            //jasjajsas
          },
        ),
      ),
    );
  }
}
